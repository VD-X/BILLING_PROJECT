# Example secrets.toml file for Streamlit Cloud deployment
# Rename this file to secrets.toml and add your actual credentials

[email]
sender_email = "your-email@gmail.com"
sender_password = "your-app-password"
hashed_code = "your-security-code-hash"  # This is generated when you set up credentials locally
